apiVersion : v1
kind  : Pod
metadata :
    name : webapp-pod
    labels :
        component : webapp
spec :
    containers :
        - name : webapp
          image : vinothrao247/webapp5
          ports :
            - containerPort : 80
